#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;

int main()
{
  int t;
  cin >> t;
  for (int i = 0; i < t; i++)
  {
    int n, a, b;
    cin >> n >> a >> b;

    pow((n - a), 2) * pow((n - b), 2);
  }
}
